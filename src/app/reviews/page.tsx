import type { Metadata } from "next";
import { Quote, Star } from "lucide-react";
import { PageHero } from "@/components/ui/page-hero";
import { Badge } from "@/components/ui/badge";
import { reviews } from "@/lib/data/content";

export const metadata: Metadata = { title: "Verified Coaching Reviews", description: "Read verified feedback attached to completed paid Dota 2 coaching orders.", alternates: { canonical: "/reviews" } };
export default function ReviewsPage() { return <><PageHero eyebrow="Verified feedback" title="Reviews with a completed order behind them." description="A review receives the verified label only when it is linked to a completed, paid service." aside={<div className="grid grid-cols-2 gap-3"><div className="surface rounded-xl p-4"><p className="text-3xl font-black">4.97</p><p className="mt-1 text-xs text-mist">Average sample rating</p></div><div className="surface rounded-xl p-4"><p className="text-3xl font-black">100%</p><p className="mt-1 text-xs text-mist">Verified shown here</p></div></div>} /><section className="section-pad container-shell"><div className="grid gap-5 lg:grid-cols-3">{[...reviews, ...reviews].map((review, index) => <article key={`${review.id}-${index}`} className="surface flex min-h-80 flex-col rounded-[1.5rem] p-6"><div className="flex items-center justify-between"><Quote className="size-7 text-crimson/50" /><Badge tone="cyan">Verified order</Badge></div><div className="mt-6 flex gap-1">{Array.from({ length: review.rating }).map((_, star) => <Star key={star} className="size-3.5 fill-amber text-amber" />)}</div><blockquote className="mt-5 text-base leading-7">“{review.quote}”</blockquote><footer className="mt-auto border-t border-white/[0.08] pt-5"><p className="text-sm font-bold">{review.customer}</p><p className="mt-1 text-xs text-mist">{review.service} with {review.coach} · {review.rank} {review.role}</p></footer></article>)}</div><p className="mt-8 text-center text-xs text-mist">Development seed reviews are clearly synthetic. Production moderation retains original content and audit history.</p></section></>; }
