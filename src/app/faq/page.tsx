import type { Metadata } from "next";
import { PageHero } from "@/components/ui/page-hero";
import { faqs } from "@/lib/data/content";

export const metadata: Metadata = { title: "Frequently Asked Questions", description: "Answers about Dota 2 coaching, account safety, scheduling, payments, verification, privacy, and refunds.", alternates: { canonical: "/faq" } };
export default function FaqPage() { const schema = { "@context": "https://schema.org", "@type": "FAQPage", mainEntity: faqs.map((faq) => ({ "@type": "Question", name: faq.question, acceptedAnswer: { "@type": "Answer", text: faq.answer } })) }; return <><PageHero eyebrow="Frequently asked questions" title="Clear before checkout." description="Account safety, scheduling, payments, verification, and support—answered without fine-print language." /><section className="section-pad container-shell"><div className="mx-auto max-w-4xl divide-y divide-white/[0.09] border-y border-white/[0.09]">{faqs.map((faq, index) => <details key={faq.question} className="group py-6" open={index === 0}><summary className="flex list-none items-center justify-between gap-7 text-lg font-bold marker:hidden"><span>{faq.question}</span><span className="text-2xl font-light text-crimson transition group-open:rotate-45">+</span></summary><p className="max-w-3xl pt-5 text-sm leading-7 text-mist">{faq.answer}</p></details>)}</div></section><script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(schema) }} /></>; }
